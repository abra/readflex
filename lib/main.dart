// Entry point of the application.
// Delegates immediately to starter() to keep main() free of any logic —
// this makes it easy to swap the entry point in tests or flavors.

import 'package:nota/app/starter.dart';

void main() => starter();
