name: review_scheduler
description: "Shared FSRS v6 review scheduling logic used by all repositories."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  domain_models:
    path: ../domain_models
  fsrs: ^2.0.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
