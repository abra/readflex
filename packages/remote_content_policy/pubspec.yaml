name: remote_content_policy
description: Validation policy for application-initiated remote content fetches.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.1

dev_dependencies:
  test: ^1.31.0
  lints: ^6.1.0
