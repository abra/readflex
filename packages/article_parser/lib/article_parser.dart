/// Article fetching + on-device readability extraction + content
/// sanitisation. The output is safe to embed in the reader WebView.
library;

export 'src/article_html_sanitizer.dart';
export 'src/article_parser.dart';
