// Screen lifecycle controls for active reading sessions.
//
// The production implementation keeps the device awake only while the reader
// explicitly marks bare reading content as active.

export 'src/screen_control_service.dart';
export 'src/wakelock_screen_control_service.dart';
