name: article_extraction_service
description: "Remote article extraction client for cleaned reader content."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  domain_models:
    path: ../domain_models
  http: ^1.6.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.31.2
