name: reader_server
description: "Local HTTP server for serving book files and article HTML to the reader WebView."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  monitoring:
    path: ../monitoring
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  http: ^1.6.0
