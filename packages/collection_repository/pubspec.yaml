name: collection_repository
description: "Manual library collection domain logic."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  domain_models:
    path: ../domain_models
  local_storage:
    path: ../local_storage
  drift: ^2.34.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  sqlite3: ^3.3.4
