name: local_storage
description: "Single Drift database shared by all repositories."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.34.1
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.6
  path: ^1.9.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  drift_dev: ^2.34.2+1
  build_runner: ^2.15.0
  sqlite3: ^3.3.4
