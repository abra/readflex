// Translate feature: bottom sheet that translates a text selection and
// optionally saves the result to the dictionary.
//
// Exposes a TextAction implementation (`TranslateAction`) that plugs into
// the reader's context panel, plus the sheet entry point used by other
// surfaces that want to translate arbitrary text.

export 'src/translate_action.dart';
export 'src/translate_sheet.dart';
