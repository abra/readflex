name: monitoring
description: "Monitoring contracts, logging infrastructure, and GlitchTip error reporting for Readflex."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  clock: ^1.1.2
  sentry: ^9.26.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
