name: fsrs_repository
description: "Centralized FSRS review state storage and scheduling."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  domain_models:
    path: ../domain_models
  local_storage:
    path: ../local_storage
  review_scheduler:
    path: ../review_scheduler
  drift: ^2.23.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  sqlite3: ^2.7.0
