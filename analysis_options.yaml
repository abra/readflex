include: package:flutter_lints/flutter.yaml

linter:
  rules:

formatter:
  trailing_commas: preserve

analyzer:
  exclude:
    - "**/*.*.dart"
    - "**/generated/**"
    # Standalone tools are analyzed from their own package context.
    - "tool/**"
